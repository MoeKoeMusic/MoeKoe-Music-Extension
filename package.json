{
  "name": "moekoe-music-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:app": "npm --prefix MoeKoeMusic install && npm --prefix MoeKoeMusic/api install",
    "generate:api": "node scripts/generate-extension-api-entry.mjs",
    "build": "node scripts/build-extension.mjs"
  },
  "dependencies": {
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "esbuild": "^0.25.11",
    "vite": "^7.2.6"
  }
}

